apiVersion: v1
kind: PersistentVolume
metadata:
  name: job-scraper-claude-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: "/mnt/data/job-scraper-claude"
