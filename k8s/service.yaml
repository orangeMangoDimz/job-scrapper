apiVersion: v1
kind: Service
metadata:
  name: job-scraper-mcp-service
spec:
  selector:
    app: job-scraper-mcp
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
